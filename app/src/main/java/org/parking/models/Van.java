package org.parking.models;

import org.parking.enums.Size;

public class Van extends Vehicle{

    public Van(final String name, final String regNum, final Size size) {
        super(name, regNum, size);
    }
}
